{{ attach_library('spokes/hero') }}

<section class="hero">
  {% if hero.media %}
    {%
      include '@spokes/media/media.twig' with {
        "image": hero.media.image
      } only
    %}
  {% endif %}

  <div class="hero__content">

    <div class="content">
      {% if hero.heading %}
        {%
          include '@spokes/heading/heading.twig' with {
            "heading": hero.heading
          } only
        %}
      {% endif %}

      {% if hero.eyebrow %}
        {%
          include '@spokes/eyebrow/eyebrow.twig' with {
            "eyebrow": hero.eyebrow
          } only
        %}
      {% endif %}

      {% if hero.body %}
        {%
          include '@spokes/body-text/body-text.twig' with {
            "body_text": hero.body.body_text,
            "modifier": hero.body.classes
          } only
        %}
      {% endif %}

      {% if hero.button.url %}
        {%
          include '@spokes/button/button.twig' with {
            "button": hero.button
          } only
        %}
      {% endif %}
    </div>
  </div>
</section>
